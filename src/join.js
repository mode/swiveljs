swivel.join = function() {
  var conds = {};
  var type  = swivel.join.types.LEFT;

  var _join = {
  };

  // Public

  // Private

  return _join;
};

swivel.join.types = {
  LEFT:  'LEFT',
  RIGHT: 'RIGHT',
  INNER: 'INNER'
};
